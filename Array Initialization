#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int A[n];
    printf("enter the array elemnts:");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&A[i]);
    }
    for(int i=0;i<n;i++)
    {
        printf("\n%d",A[i]);
    }
}
